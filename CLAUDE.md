# Vendus CLI

CLI tool for querying Cegid Vendus POS data. Install with `pip install -e .`, then use the `vendus-pos` command.

See `SKILL.md` for the complete command reference, date aliases, output formats, and how to answer common POS questions using this tool.
